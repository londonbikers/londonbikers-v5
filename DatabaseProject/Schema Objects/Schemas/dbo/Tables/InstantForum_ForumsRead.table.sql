/*CREATE TABLE [dbo].[InstantForum_ForumsRead] (
    [ForumsReadID] INT      IDENTITY (1, 1) NOT NULL,
    [ReadForumID]  INT      NOT NULL,
    [ReadUserID]   INT      NOT NULL,
    [ReadDate]     DATETIME /*DEFAULT (getdate())*/ NOT NULL,
    [Expires]      DATETIME /*DEFAULT (getdate())*/ NOT NULL
);*/

